structure simple_game
  instances
    b1, b2, b3 in block
    c1, c2, c3, c4 in cell

  value of statics
    symmetric_connectivity.
    adjacent(c1, c2).
    adjacent(c1, c3).
    adjacent(c2, c4).
    adjacent(c3, c4).
    weight(b1, 3).
    weight(b2, 5).
    weight(b3, 1).